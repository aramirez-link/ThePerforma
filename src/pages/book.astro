---
import BaseLayout from "../layouts/BaseLayout.astro";
import Section from "../components/Section.astro";
import Card from "../components/Card.astro";
import Button from "../components/Button.astro";
import BookingConciergeEntry from "../components/BookingConciergeEntry";
---
<BaseLayout title="VIP Reservation | The Performa" description="Concierge-level availability request for promoters and agents.">
  <section class="mx-auto max-w-6xl px-6 pt-8">
    <div class="relative overflow-hidden rounded-[2rem] border border-white/15">
      <img src="/assets/img/137NewChip_4K.jpg" alt="" aria-hidden="true" class="h-[42vh] w-full object-cover opacity-40" />
      <div class="absolute inset-0 bg-[linear-gradient(180deg,rgba(7,7,11,0.3),rgba(7,7,11,0.92))]"></div>
      <div class="absolute inset-0 flex items-end p-8">
        <div>
          <p class="text-xs uppercase tracking-[0.34em] text-gold/90">VIP Reservation</p>
          <h1 class="mt-3 font-display text-4xl md:text-5xl">Chip Lee - Concierge Booking Deck</h1>
          <p class="mt-3 max-w-2xl text-sm text-white/75 md:text-base">Choose your venue profile, confirm production tier, and request availability through a premium promoter flow.</p>
        </div>
      </div>
    </div>
  </section>
  <Section
    eyebrow="EPK"
    title="Promoter Cockpit"
    subtitle="Fast facts, tech rider snapshot, press assets, and a direct inquiry flow for booking Chip Lee."
  >
    <div class="grid gap-6 md:grid-cols-3">
      <Card title="Set Formats" subtitle="60-90 minute headline set. Festival-ready flow. Built for mainstage impact." />
      <Card title="Tech Rider" subtitle="Stage plot, I/O, and lighting notes available upon request. Streamlined for fast load-in." />
      <Card title="Press Assets" subtitle="Download high-res photos, logos, and approved press copy for Chip Lee - The Performa." />
    </div>
    <div class="mt-10 flex flex-wrap gap-4 items-center">
      <Button href="/media/press-kit.pdf" variant="primary">Download Press Kit</Button>
      <BookingConciergeEntry client:load />
      <Button href="mailto:booking@theperforma.example" variant="ghost">Email Bookings</Button>
    </div>
    <div class="mt-12 rounded-3xl border border-white/15 bg-black/50 p-6 shadow-[0_0_70px_rgba(243,211,139,0.12)]">
      <h3 class="text-xl font-semibold">Booking Inquiry</h3>
      <p class="mt-2 text-sm text-white/70">Submit a request and our team will respond with availability, routing, and production requirements.</p>
      <form class="mt-6 grid gap-4 md:grid-cols-2" action="/" method="post">
        <input class="rounded-2xl border border-white/20 bg-black/40 px-4 py-3" name="name" placeholder="Promoter / Organization" required />
        <input class="rounded-2xl border border-white/20 bg-black/40 px-4 py-3" name="email" placeholder="Email" required />
        <input class="rounded-2xl border border-white/20 bg-black/40 px-4 py-3" name="date" placeholder="Event Date" />
        <input class="rounded-2xl border border-white/20 bg-black/40 px-4 py-3" name="venue" placeholder="Venue / City" />
        <textarea class="md:col-span-2 rounded-2xl border border-white/20 bg-black/40 px-4 py-3" name="details" rows="4" placeholder="Show Details (capacity, set length, production, timing)"></textarea>
        <button type="submit" class="rounded-full bg-ember px-6 py-3 text-xs uppercase tracking-[0.4em] text-ink">
          Send Booking Request
        </button>
      </form>
      <p class="mt-3 text-xs text-white/50">TODO: Replace action URL with preferred form endpoint.</p>
    </div>
  </Section>
</BaseLayout>

